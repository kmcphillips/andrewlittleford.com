.post
  %h2.title Edit Discography Entry
  .entry
    .meta
    = render 'form'

    .image_links
      = index_entity_image admin_discography_entries_path, "Back to Discography Index"
      %br
      = destroy_entity_image admin_discography_entry_path(@entry), "Delete Entry"
