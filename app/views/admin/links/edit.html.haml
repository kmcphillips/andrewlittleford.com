.post
  %h2.title Edit Link
  .entry
    .meta

    = render 'form'

    .image_links
      = index_entity_image admin_links_path, "Back to Links Index"
      %br
      = destroy_entity_image admin_link_path(@link), "Delete Link"

