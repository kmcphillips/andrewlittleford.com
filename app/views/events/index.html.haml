- if @current.any?
  %h2.super Happening Right Now
  - @current.each do |event|
    = render :partial => "event", :object => event

- if @upcoming.any?
  %h2.super Upcoming Shows
  - @upcoming.each do |event|
    = render :partial => "event", :object => event

%h2.super Past Shows
- @past.each do |event|
  = render :partial => "event", :object => event
